pipeline {
    agent any
    
    stages {
      stage ("Deploying to Development server") {
        	steps {
                ssh -i key.pem ${REMOTE_USER}@${REMOTE_HOST} "ls"
                }

            	
          }
      }
    }
}
