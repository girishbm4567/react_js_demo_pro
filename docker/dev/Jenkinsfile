pipeline {
    agent any
    
    stages {
      stage ("Deploying to Development server") {
        	steps {
                sshagent(['<SSH credentials ID>']) {
                    sh """
                        ssh ${REMOTE_USER}@${REMOTE_HOST} 'ls'
                    """
                }
            	
          }
      }
    }
}
